# Backend API (optional; defaults to /api)
# VITE_API_BASE_URL=https://your-api.example.com

# YouTube Data API v3 key — enables real search results for Verify Notes → Related topics.
# Without it, the app uses noembed + curated fallback video IDs.
# VITE_YOUTUBE_API_KEY=
